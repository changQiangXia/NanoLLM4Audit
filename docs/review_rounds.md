# 审视与迭代记录

## 第一轮：目录与流程

- 目录收敛到 `configs`、`data`、`docs`、`scripts`、`src`
- 长链路模块压缩到单层包结构
- 外部 API 调用替换为本地模板化摘要

## 第二轮：结果表达

- 图表数量扩展到九张
- 增加 HTML 看板，支持集中查看实验结果
- 报告内容增加阶段汇总与实验对比表

## 第三轮：稳定复现

- 数据集改为固定随机种子生成
- 检查脚本增加关键产物校验
- 启动脚本压缩为一条命令

## 第四轮：机器学习补充

- 增加浅层决策树分类器
- 增加特征重要性图、树结构图、树分数分布图
- 增加 Holdout 评估、树规则文本与模型指标文件
